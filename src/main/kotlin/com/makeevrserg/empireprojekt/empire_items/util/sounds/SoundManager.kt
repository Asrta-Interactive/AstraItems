package com.makeevrserg.empireprojekt.empire_items.util.sounds

import com.makeevrserg.empireprojekt.empire_items.util.EmpireSound

class SoundManager {
    val soundsList = EmpireSound.new()
    val soundByID = EmpireSound.soundByID(soundsList)
}